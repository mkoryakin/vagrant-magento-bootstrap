# vagrant-magento-bootstrap
pls add domain, upassword and mysql_root_password vars into magento/group_vars/{all,db} files, do "vagrant up" and then "cd /vagrant/magento;ansible-playbook -i hosts site.yml" 
